## 1. 关于创建数组
这一点总是会混淆，之前学过一些java，然后cpp基础学的不牢固，导致经常在初始化数组的时候犯错，故在此总结一下

---

### 在栈上创建数组
在栈上创建数组可以直接：
```c++
int p[20]; //直接创建
//如果希望初始化则可以
int arr[3] = {0, 3, 5};
```

### 在堆上创建数组
一般都是定义指针进行创建，指定数组空间大小即可
```c++
int *p = new int[20];
```

## 2.关于接收输入

---

### cin接收输入：接收单个值（数字、单词）
cin接收输入格式： cin >> k;

">>" 运算符会自动跳过所有开头的空白（空格、Tab、换行），然后开始读取，直到再次遇到空白为止。因此对于一行的输入之间有空格的情况，cin只能够读取到第一个，此时就需要getline这个函数

### getline()接收输入：会读取一整行输入，直到遇到换行符（按 Enter 键）为止
getline函数接收输入格式，例如需要写入名字为Zhang San
```c++
std::string fullName;
// 语法: std::getline(从哪里读, 存到哪里);
std::getline(std::cin, fullName); //读取一整行
```

### 误区：cin和getline混用
💡 底层问题：
你使用 std::cin >> age; 输入 25 然后按了 Enter。
">>" 运算符只“提取”了 25，那个换行符 (\n) 仍然残留在输入流（缓冲区）里。
你接着调用 std::getline(std::cin, fullName);。
std::getline 读到的第一个字符就是那个残留的 \n！它以为“这一行已经结束了”，于是它立刻停止，什么也没读到。
✅ 解决方案： 在 getline 之前，手动清除那个残留的换行符。
```c++
#include <iostream>
#include <string>
#include <limits> // 必须包含这个头文件

int main() {
    int age;
    std::string fullName;

    std::cout << "请输入你的年龄: ";
    std::cin >> age;

    // --- 关键修复 ---
    // 忽略掉 cin 缓冲区里残留的所有字符，
    // 直到遇到换行符 '\n' 为止。
    std::cin.ignore(std::numeric_limits<std::streamsize>::max(), '\n');
    // --- 修复结束 ---

    std::cout << "请输入你的全名: ";
    std::getline(std::cin, fullName);

    std::cout << "你好, " << fullName << " (年龄: " << age << ")" << std::endl;
    return 0;
}
```

### 总结
读数字或单个单词： 用 std::cin >> 变量;

读一整行（带空格）： 用 std::getline(std::cin, 字符串变量);

如果 cin >> 后面要接 getline： 必须在中间加 std::cin.ignore(...); 来“清空”换行符。

---
